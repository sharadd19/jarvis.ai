{
  "name": "my-app",
  "version": "1.0.0",
  "scripts": {
    "app": "npx concurrently \"npm run dev --prefix frontend\" \"make -C backend run\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}